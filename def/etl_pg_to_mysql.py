import os
import pandas as pd

class pg_to_mysql():
   def __init__(self):
      db_helper = db_helper()

   def schema_apply(self, source_table_name, dest_table_name, database, username, password, port):
      db

